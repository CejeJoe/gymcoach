# Server
DATABASE_URL=postgres://user:pass@host:5432/dbname
JWT_SECRET=change-me

# Caching / bots
NOINDEX=false

# Features
EMAIL_VERIFY=true
APPROVAL_REQUIRED=true
# Admin PATCH endpoints are disabled by default for Phase 1 (read-only)
ENABLE_ADMIN_MUTATION=false
ENABLE_SCHEDULER=false

# Email provider (Phase 2 - wire up actual sender)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=secret
# FROM_EMAIL=no-reply@gymcoach.app
# ADMIN_EMAIL=owner@example.com
